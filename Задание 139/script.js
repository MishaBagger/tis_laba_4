"use strict";

// 1 task
let num = 25619;
let str = String(num)
let last = str[str.length - 1];

if (last == 0) {
	console.log('ravno 0');
} else {
	console.log('ne ravno 0');
}

// 2 task
let num2 = 13;
let str2 = String(num2)
let last2 = str2[str2.length - 1];

if (last2 == 0 || last2 == 2 || last2 == 4 || last2 == 6 || last2 == 8) {
	console.log('chetnoe');
} else {
	console.log('ne chetnoe');
}

// 3 task